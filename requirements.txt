# Runtime dependencies for the interview web app.
# Kept intentionally small so the Vercel function stays under the size limit —
# the heavy Vertex AI / Together / AWS / notebook packages the research code can
# optionally use are imported lazily and only installed if you actually need them.

# Web
flask==3.1.2
flask-cors==6.0.1

# OpenAI + LangChain (the models we run in production)
openai==1.54.0
langchain-core==0.3.15
langchain-openai==0.2.5
tiktoken==0.8.0

# Vector store + numerics (memory / question banks)
faiss-cpu==1.9.0
numpy==2.1.3

# Sentiment for the engagement monitor (VADER)
nltk==3.9.1

# Storage / research DB (Vercel Postgres)
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Utilities
python-dotenv==1.0.1
requests==2.32.3
pydantic==2.9.2
PyYAML==6.0.2

# Pinned transitives — newer versions break openai==1.54.0 / langchain-core 0.3.15
# (e.g. httpx dropped the `proxies` kwarg openai 1.54 passes).
httpx==0.27.2
httpcore==1.0.6
langsmith==0.1.139
